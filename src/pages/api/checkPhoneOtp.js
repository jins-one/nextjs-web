import getConfig from "next/config";

export default function checkPhoneOtp(to, authNum){

    try{
        

        console.log(phoneAuthMap);

        return { success : true }
    }
    catch(err) {
        console.log(err);
    }
    
}